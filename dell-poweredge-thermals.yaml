name: dell-poweredge-thermals
container:
  entrypoint: ./dell-poweredge-thermals
  args:
    - watch
mounts:
  - source: /dev/ipmi0
    destination: /dev/ipmi0
    type: bind
    options:
      - bind
restart: always