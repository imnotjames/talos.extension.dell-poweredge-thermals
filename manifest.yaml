version: v1alpha1
metadata:
  name: dell-poweredge-thermals
  version: "VERSION"
  author: imnotjames
  description: >
    Manage fan speed based on the current temperature
    of a Dell PowerEdge server over IPMI.
  compatibility:
    talos:
      version: ">= v1.5.0"
